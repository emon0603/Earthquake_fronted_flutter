import 'package:flutter/material.dart';

class EarthquakePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('EarthquakePage', style: TextStyle(fontSize: 24)),
    );
  }
}